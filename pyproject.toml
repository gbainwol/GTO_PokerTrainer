[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pokertrainer"
version = "0.1.0"
description = "CFR+/DCFR solver scaffolding with HTTP and gRPC services"
authors = [{name="PokerTrainer"}]
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
